#include <cs50.h>
#include <stdio.h> //header files. standard input and output (io) library

int main (void)
{
        for (int i = 0; i < 3; i++)
        {
                printf("meow\n");
        }

}

